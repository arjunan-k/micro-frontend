const InvoiceApp = () => {
  return (
    <div className="p-32">
      <h1>Invoice Repository</h1>
      <p>
        Create a repository for Invoice team. Then generate the remoteEntry.js
        and expose it. Consume it in host app & use the component in the same
        way we did for Payment and Wallet.
      </p>
    </div>
  );
};

export default InvoiceApp;
